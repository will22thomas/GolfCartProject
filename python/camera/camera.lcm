package camera;

// Live streaming data in an array over lcm

struct camera_image_t {
    string timestamp;
    string camera_name;
    int64_t framecount;

    int32_t width;
    int32_t height;
    int32_t row_stride;

    int32_t size;
    byte data[size];
}